# Elasticsearch

```curl

curl \-X GET "localhost:9200/\_search?pretty" \-H 'Content-Type: application/json' \-d '{
	"query": {
		"script\_score": {
			"query": {
				"match\_all": {}
			},
		"script": {
			"source": "1 / (1 + l2norm(params.query\_vector, 'image\_vec'))",
			"params": {
				"query_vector": [1,2,3]
			}
		}
	}
}
}'

```

# Index

## Create

## Get

### Get All

`curl http://localhost:9200/_aliases`
`curl http://localhost:9200/_cat/indices`

### Get By ID

## Update

## Delete

```
curl -X DELETE http://localhost:9200/INDEX_NAME
```

## Add document

## Update Document

## Delete Document

## Search for Document

### Search using Query

### Search using script query

`curl es:9200/txtsearch/_search?q=COVID`
`curl es:9200/txtsearch/_search?q=बाबरी मस्जिद के लिए`

# Count

`culr es:9200/imgsearch/_count`
