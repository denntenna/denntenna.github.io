# ElasticSearch

# Cheat Sheet

```curl

curl -X GET "localhost:9200/_search?pretty" -H 'Content-Type: application/json' -d '{
	"query": {
		"script_score": {
			"query": {
				"match_all": {}
			},
		"script": {
			"source": "1 / (1 + l2norm(params.query_vector, 'image_vec'))",
			"params": {
				"query_vector": [1,2,3]
			}
		}
	}
}
}'

```

# Index

## Create

## Get

### Get All

`curl http://localhost:9200/_aliases`
`curl http://localhost:9200/_cat/indices`

### Get By ID

## Update

## Delete

```
curl -X DELETE http://localhost:9200/INDEX_NAME
```

## Add document

## Update Document

## Delete Document

## Search for Documemt

### Search using Query

### Search using script query
