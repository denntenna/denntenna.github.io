# NPM

`npm outdated` : shows all outdated packages in your current project

## Upgrading packages 
https://docs.npmjs.com/cli/v6/commands/npm-update


˜ : next 
^ : major version

`npm install lodash@latest`

## Inspect Dependencies
```
npm list
npm list $dependency_name
npm list --depth=$depth
```